services:
  - type: web
    name: rss-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi_teste:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9